#include <topcoder.h>

/*
 * 4以上ということは、3cut中に
 * 端点を2カ所決めて、縦に区切る
 * そのときの最小値を求める
 * 別々に切った場合に4つ見つかれば、それにあわせて3cutすればOK

  if (val>=c) G++,last=l; 
  } 
  if (G>=4) return 1; 
  } 
 */

int run(){

}

int main()
{
  
  return 0;
}
